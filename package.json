{
  "name": "node-server-key-generator",
  "version": "1.0.0",
  "description": "A Node.js utility to generate mutual TLS certificates and elliptic curve (EC) JWK key sets (JWKS) for secure server-to-server communication and JWT-based authentication.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node generate-tls-certs.js && node generate-jwk.js",
    "start:tls": "node generate-tls-certs.js",
    "start:jwk": "node generate-jwk.js"
  },
  "author": "Ram",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "jose": "^6.0.12"
  },
  "type": "module"
}
