# TLS Certificate Configuration
# Directory to store generated keys
KEY_DIR=keys

# Common Name for service certificate
CN=internal-service

# DNS Subject Alternative Name
SAN_DNS=localhost

# IP Subject Alternative Name
SAN_IP=127.0.0.1
